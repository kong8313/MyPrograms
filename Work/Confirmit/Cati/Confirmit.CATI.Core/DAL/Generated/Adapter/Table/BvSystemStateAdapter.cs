//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a EntityAdapterTemplate.tt T4 template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//     
//     DO NOT EDIT THIS FILE MANUALLY
// </auto-generated>
//------------------------------------------------------------------------------

// Disable missing XML comment for publicly visible type or member
#pragma warning disable 1591

using System;
using System.Data;
using System.Data.SqlClient;
using System.Collections.Generic;

using Confirmit.CATI.Common;
using Confirmit.CATI.Core.DAL.Framework;
using Confirmit.CATI.Core.DAL.Generated.Entity.Table;
using Confirmit.CATI.Core.Misc;
using Confirmit.CATI.Common.ServiceLocation;
using Confirmit.CATI.Core.SystemSettings;


namespace Confirmit.CATI.Core.DAL.Generated.Entity.Adapter
{
            
            

    public class BvSystemStateAdapter
    {
        public static readonly string selectSql =
            "SELECT\r\n" +
                "[SystemName],\r\n" +
                "[Value]\r\n" +
            "FROM\r\n" +
                "dbo.[BvSystemState]";

		public static readonly string mergeSql = 
			"MERGE dbo.[BvSystemState] as target\r\n" +
			    "USING\r\n" +
			    "(\r\n" + 
			        "VALUES\r\n" +
			        "(\r\n" + 
			            "@SystemName,\r\n" +
                        "@Value" + 
					")" +
				")" +
			    "AS source\r\n" +
				"(\r\n" +
				    "SystemName,\r\n" +
                    "Value" +
				")\r\n" +
			    "ON\r\n" +
				"(\r\n" +
				    "target.[SystemName] = source.[SystemName]\r\n" +
				")\r\n" +
                "WHEN MATCHED THEN UPDATE SET\r\n" +
					"target.[Value] = source.[Value]\r\n" +
				"WHEN NOT MATCHED BY TARGET THEN INSERT\r\n" +
				"(\r\n" + 
					"[SystemName],\r\n" +
                    "[Value]\r\n" +
				")\r\n" +
				"VALUES\r\n" +
				"(\r\n" +
					"SystemName,\r\n" +
                    "Value\r\n" +
				");";

        public static readonly string insertSql =
            "INSERT dbo.[BvSystemState]\r\n"+
            "(\r\n" + 
                "[SystemName],\r\n" +
                "[Value]\r\n" +
            ")\r\n" +
            "VALUES\r\n" +
            "(\r\n" +
                "@SystemName,\r\n" +
                "@Value\r\n" +
            ")";

        public static readonly string deleteSql = 
            "DELETE FROM dbo.[BvSystemState]\r\n";

        public static readonly string deleteAndOutputSql = 
            "SELECT * INTO #BvSystemStateOutput FROM dbo.[BvSystemState] WHERE 1<>1\r\n" +
            "\r\n" +
            "DELETE FROM dbo.[BvSystemState] OUTPUT DELETED.[SystemName],\r\n" +
                    "DELETED.[Value]\r\n" + 
			"INTO #BvSystemStateOutput(\r\n" +
                    "[SystemName],\r\n" +
                    "[Value]\r\n" +
			")\r\n" +
			"%WHERE_CLAUSE%" +
            "\r\n" +
            "SELECT\r\n" +
				 "[SystemName],\r\n" +
                "[Value]\r\n" +
            "FROM\r\n" +
             "    #BvSystemStateOutput\r\n" +
            "\r\n" +
             "DROP TABLE #BvSystemStateOutput";

        public static readonly string updateSql =
            "UPDATE dbo.[BvSystemState] SET\r\n" +
                "[Value] = @Value\r\n";

        public static readonly string updateAndOutputSql =
            "SELECT * INTO #BvSystemStateOutput FROM dbo.[BvSystemState] WHERE 1<>1\r\n" +
            "\r\n" +
            "UPDATE dbo.[BvSystemState] SET\r\n" +
                "[Value] = @Value\r\n" +
            "OUTPUT INSERTED.* INTO #BvSystemStateOutput\r\n" +
             "%WHERE_CLAUSE%" +
            "\r\n" +
            "SELECT\r\n" +
                 "[SystemName],\r\n" +
                "[Value]\r\n" +
            "FROM\r\n" +
             "    #BvSystemStateOutput\r\n" +
            "\r\n" +
             "DROP TABLE #BvSystemStateOutput";


        public static readonly string updateDefaultConditionSql =
                "[SystemName] = @SystemName\r\n";
		
		public static readonly bool IsUpdateSupported = true;

        public static BvSystemStateEntity Read(IDataReader rd)
        {
            bool isRead = rd.Read();

            if (isRead == false)
                return null;

			return ReadEntity(rd);
		}

		public static BvSystemStateEntity ReadEntity(IDataReader rd)
		{
            var entity = new BvSystemStateEntity();

            int SystemNameOrdinal = rd.GetOrdinal("SystemName");

            if (!rd.IsDBNull(SystemNameOrdinal))
                entity.SystemName = 
                    rd.GetString(SystemNameOrdinal);

            int ValueOrdinal = rd.GetOrdinal("Value");

            if (!rd.IsDBNull(ValueOrdinal))
                entity.Value = 
                    rd.GetString(ValueOrdinal);

            return entity;
        }

        [NotNull]
        public static List< BvSystemStateEntity > ReadList(IDataReader rd)
        {
            var BvSystemStateEntityList = new List< BvSystemStateEntity >();

            int SystemNameOrdinal = rd.GetOrdinal("SystemName");
            int ValueOrdinal = rd.GetOrdinal("Value");

            while(true)
            {
                bool isRead = rd.Read();

                if (isRead == false)
                    break;

                var entity = new BvSystemStateEntity();

                if (!rd.IsDBNull(SystemNameOrdinal))
                    entity.SystemName = 
                        rd.GetString(SystemNameOrdinal);

                if (!rd.IsDBNull(ValueOrdinal))
                    entity.Value = 
                        rd.GetString(ValueOrdinal);


                BvSystemStateEntityList.Add(entity);
            }

            return BvSystemStateEntityList;
        }
		
        [NotNull]
        public static List< BvSystemStateEntity > GetAll()
        {
            return GetByCondition(null);
        }
		
        [NotNull]
        public static List< BvSystemStateEntity > GetAllForCache()
        {
            return GetByCondition(null, true);
        }
		
        [NotNull]
        public static List< BvSystemStateEntity > GetByCondition(
            string condition,
            params SqlParameter[] parameters)
        {
             return GetByCondition(condition, false, parameters);
        }
		
        [NotNull]
        public static List< BvSystemStateEntity > GetByCondition(
            string condition,
            bool useNewConnection,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                if(useNewConnection)
                {
                    var connection = new SqlConnection(BackendInstance.Current.ConnectionString);
                    disposableResources.Push(connection);
                    connection.Open();
                    command.Connection = connection;
                }
                else
                {
                    var connectionScope = new ConnectionScope();
                    disposableResources.Push(connectionScope);

                    command.Connection = connectionScope.Connection;

                    if (DatabaseTransactionScope.Current != null)
                    {
                        command.Transaction = DatabaseTransactionScope.Current.Transaction;
                    }
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = selectSql;

                if (condition != null)
                {
                    command.Parameters.AddRange(parameters);
                    command.CommandText += "\r\nWHERE\r\n" + condition;
                }

                var reader = command.ExecuteReader();

                disposableResources.Push(reader);

                return ReadList(reader);
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

        public static void Insert([NotNull] BvSystemStateEntity entity)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();
                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = insertSql;

                command.Parameters.Add(new SqlParameter("SystemName", SqlDbType.NVarChar)).Value = (object)entity.SystemName ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("Value", SqlDbType.NVarChar)).Value = (object)entity.Value ?? DBNull.Value;

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

		public static void Merge([NotNull] BvSystemStateEntity entity)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();
                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = mergeSql;

                command.Parameters.Add(new SqlParameter("SystemName", SqlDbType.NVarChar)).Value = (object)entity.SystemName ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("Value", SqlDbType.NVarChar)).Value = (object)entity.Value ?? DBNull.Value;

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }
		
        [NotNull]
        public static List< BvSystemStateEntity > DeleteByConditionAndOutput(
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;
                
                command.CommandType = CommandType.Text;

                if (condition != null)
                {
                    command.CommandText = deleteAndOutputSql.Replace("%WHERE_CLAUSE%", "\r\nWHERE\r\n" + condition + "\r\n");
                    command.Parameters.AddRange(parameters);
                }
                else
                {
                    command.CommandText = deleteAndOutputSql.Replace("%WHERE_CLAUSE%", "");
                }

                var reader = command.ExecuteReader();
                disposableResources.Push(reader);

                var deletedRows = ReadList(reader);

                return deletedRows;
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }
        
        public static void DeleteByCondition(
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;
                
                command.CommandType = CommandType.Text;
                command.CommandText = deleteSql;

                if (condition != null)
                {
                    command.Parameters.AddRange(parameters);
                    command.CommandText += "\r\nWHERE\r\n" + condition;
                }

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

        public static void Update([NotNull] BvSystemStateEntity entity)
        {
            UpdateByCondition(entity, null);
        }
		
        [NotNull]
        public static List< BvSystemStateEntity > UpdateByConditionAndOutput(
            [NotNull] BvSystemStateEntity entity,
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;

                if (condition != null)
                {
                    command.CommandText = updateAndOutputSql.Replace("%WHERE_CLAUSE%", "\r\nWHERE\r\n" + condition + "\r\n");
                    command.Parameters.AddRange(parameters);
                }
                else
                {
                    command.CommandText = updateAndOutputSql.Replace("%WHERE_CLAUSE%", "");
                }

                command.Parameters.Add(new SqlParameter("SystemName", SqlDbType.NVarChar)).Value = (object)entity.SystemName ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("Value", SqlDbType.NVarChar)).Value = (object)entity.Value ?? DBNull.Value;

                var reader = command.ExecuteReader();
                disposableResources.Push(reader);

                var updatedRows = ReadList(reader);

                return updatedRows;
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }
        
        public static void UpdateByCondition(
            [NotNull] BvSystemStateEntity entity,
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = updateSql;

                if (condition == null)
                {
                    command.CommandText += "\r\nWHERE\r\n" + updateDefaultConditionSql;
                }
                else
                {
                    command.Parameters.AddRange(parameters);
                    command.CommandText += "\r\nWHERE\r\n" + condition;
                }

                command.Parameters.Add(new SqlParameter("SystemName", SqlDbType.NVarChar)).Value = (object)entity.SystemName ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("Value", SqlDbType.NVarChar)).Value = (object)entity.Value ?? DBNull.Value;

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

		[NotNull]
        public static DataTable CreateDataTable()
        {
            var table = new DataTable("BvSystemState");
            var columnSystemName = new DataColumn("SystemName", typeof(string));
            columnSystemName.AllowDBNull = false;
            table.Columns.Add(columnSystemName);

            var columnValue = new DataColumn("Value", typeof(string));
            columnValue.AllowDBNull = true;
            table.Columns.Add(columnValue);

            return table;
        }

        public static void SaveEntity2DataTable(DataTable table, BvSystemStateEntity entity)
        {
            var rowData = new object[]
                {
                    entity.SystemName,
                    entity.Value
                };

            table.Rows.Add(rowData);
        }
    }
}
