//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a EntityAdapterTemplate.tt T4 template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//     
//     DO NOT EDIT THIS FILE MANUALLY
// </auto-generated>
//------------------------------------------------------------------------------

// Disable missing XML comment for publicly visible type or member
#pragma warning disable 1591

using System;
using System.Data;
using System.Data.SqlClient;
using System.Collections.Generic;

using Confirmit.CATI.Common;
using Confirmit.CATI.Core.DAL.Framework;
using Confirmit.CATI.Core.DAL.Generated.Entity.Table;
using Confirmit.CATI.Core.Misc;
using Confirmit.CATI.Common.ServiceLocation;
using Confirmit.CATI.Core.SystemSettings;


namespace Confirmit.CATI.Core.DAL.Generated.Entity.Adapter
{
            
            

    public class BvPersonFailedLoginAttemptsAdapter
    {
        public static readonly string selectSql =
            "SELECT\r\n" +
                "[PersonId],\r\n" +
                "[Count]\r\n" +
            "FROM\r\n" +
                "dbo.[BvPersonFailedLoginAttempts]";

		public static readonly string mergeSql = 
			"MERGE dbo.[BvPersonFailedLoginAttempts] as target\r\n" +
			    "USING\r\n" +
			    "(\r\n" + 
			        "VALUES\r\n" +
			        "(\r\n" + 
			            "@PersonId,\r\n" +
                        "@Count" + 
					")" +
				")" +
			    "AS source\r\n" +
				"(\r\n" +
				    "PersonId,\r\n" +
                    "Count" +
				")\r\n" +
			    "ON\r\n" +
				"(\r\n" +
				    "target.[PersonId] = source.[PersonId]\r\n" +
				")\r\n" +
                "WHEN MATCHED THEN UPDATE SET\r\n" +
					"target.[Count] = source.[Count]\r\n" +
				"WHEN NOT MATCHED BY TARGET THEN INSERT\r\n" +
				"(\r\n" + 
					"[PersonId],\r\n" +
                    "[Count]\r\n" +
				")\r\n" +
				"VALUES\r\n" +
				"(\r\n" +
					"PersonId,\r\n" +
                    "Count\r\n" +
				");";

        public static readonly string insertSql =
            "INSERT dbo.[BvPersonFailedLoginAttempts]\r\n"+
            "(\r\n" + 
                "[PersonId],\r\n" +
                "[Count]\r\n" +
            ")\r\n" +
            "VALUES\r\n" +
            "(\r\n" +
                "@PersonId,\r\n" +
                "@Count\r\n" +
            ")";

        public static readonly string deleteSql = 
            "DELETE FROM dbo.[BvPersonFailedLoginAttempts]\r\n";

        public static readonly string deleteAndOutputSql = 
            "SELECT * INTO #BvPersonFailedLoginAttemptsOutput FROM dbo.[BvPersonFailedLoginAttempts] WHERE 1<>1\r\n" +
            "\r\n" +
            "DELETE FROM dbo.[BvPersonFailedLoginAttempts] OUTPUT DELETED.[PersonId],\r\n" +
                    "DELETED.[Count]\r\n" + 
			"INTO #BvPersonFailedLoginAttemptsOutput(\r\n" +
                    "[PersonId],\r\n" +
                    "[Count]\r\n" +
			")\r\n" +
			"%WHERE_CLAUSE%" +
            "\r\n" +
            "SELECT\r\n" +
				 "[PersonId],\r\n" +
                "[Count]\r\n" +
            "FROM\r\n" +
             "    #BvPersonFailedLoginAttemptsOutput\r\n" +
            "\r\n" +
             "DROP TABLE #BvPersonFailedLoginAttemptsOutput";

        public static readonly string updateSql =
            "UPDATE dbo.[BvPersonFailedLoginAttempts] SET\r\n" +
                "[Count] = @Count\r\n";

        public static readonly string updateAndOutputSql =
            "SELECT * INTO #BvPersonFailedLoginAttemptsOutput FROM dbo.[BvPersonFailedLoginAttempts] WHERE 1<>1\r\n" +
            "\r\n" +
            "UPDATE dbo.[BvPersonFailedLoginAttempts] SET\r\n" +
                "[Count] = @Count\r\n" +
            "OUTPUT INSERTED.* INTO #BvPersonFailedLoginAttemptsOutput\r\n" +
             "%WHERE_CLAUSE%" +
            "\r\n" +
            "SELECT\r\n" +
                 "[PersonId],\r\n" +
                "[Count]\r\n" +
            "FROM\r\n" +
             "    #BvPersonFailedLoginAttemptsOutput\r\n" +
            "\r\n" +
             "DROP TABLE #BvPersonFailedLoginAttemptsOutput";


        public static readonly string updateDefaultConditionSql =
                "[PersonId] = @PersonId\r\n";
		
		public static readonly bool IsUpdateSupported = true;

        public static BvPersonFailedLoginAttemptsEntity Read(IDataReader rd)
        {
            bool isRead = rd.Read();

            if (isRead == false)
                return null;

			return ReadEntity(rd);
		}

		public static BvPersonFailedLoginAttemptsEntity ReadEntity(IDataReader rd)
		{
            var entity = new BvPersonFailedLoginAttemptsEntity();

            int PersonIdOrdinal = rd.GetOrdinal("PersonId");

            if (!rd.IsDBNull(PersonIdOrdinal))
                entity.PersonId = 
                    rd.GetInt32(PersonIdOrdinal);

            int CountOrdinal = rd.GetOrdinal("Count");

            if (!rd.IsDBNull(CountOrdinal))
                entity.Count = 
                    rd.GetInt32(CountOrdinal);

            return entity;
        }

        [NotNull]
        public static List< BvPersonFailedLoginAttemptsEntity > ReadList(IDataReader rd)
        {
            var BvPersonFailedLoginAttemptsEntityList = new List< BvPersonFailedLoginAttemptsEntity >();

            int PersonIdOrdinal = rd.GetOrdinal("PersonId");
            int CountOrdinal = rd.GetOrdinal("Count");

            while(true)
            {
                bool isRead = rd.Read();

                if (isRead == false)
                    break;

                var entity = new BvPersonFailedLoginAttemptsEntity();

                if (!rd.IsDBNull(PersonIdOrdinal))
                    entity.PersonId = 
                        rd.GetInt32(PersonIdOrdinal);

                if (!rd.IsDBNull(CountOrdinal))
                    entity.Count = 
                        rd.GetInt32(CountOrdinal);


                BvPersonFailedLoginAttemptsEntityList.Add(entity);
            }

            return BvPersonFailedLoginAttemptsEntityList;
        }
		
        [NotNull]
        public static List< BvPersonFailedLoginAttemptsEntity > GetAll()
        {
            return GetByCondition(null);
        }
		
        [NotNull]
        public static List< BvPersonFailedLoginAttemptsEntity > GetAllForCache()
        {
            return GetByCondition(null, true);
        }
		
        [NotNull]
        public static List< BvPersonFailedLoginAttemptsEntity > GetByCondition(
            string condition,
            params SqlParameter[] parameters)
        {
             return GetByCondition(condition, false, parameters);
        }
		
        [NotNull]
        public static List< BvPersonFailedLoginAttemptsEntity > GetByCondition(
            string condition,
            bool useNewConnection,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                if(useNewConnection)
                {
                    var connection = new SqlConnection(BackendInstance.Current.ConnectionString);
                    disposableResources.Push(connection);
                    connection.Open();
                    command.Connection = connection;
                }
                else
                {
                    var connectionScope = new ConnectionScope();
                    disposableResources.Push(connectionScope);

                    command.Connection = connectionScope.Connection;

                    if (DatabaseTransactionScope.Current != null)
                    {
                        command.Transaction = DatabaseTransactionScope.Current.Transaction;
                    }
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = selectSql;

                if (condition != null)
                {
                    command.Parameters.AddRange(parameters);
                    command.CommandText += "\r\nWHERE\r\n" + condition;
                }

                var reader = command.ExecuteReader();

                disposableResources.Push(reader);

                return ReadList(reader);
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

        public static void Insert([NotNull] BvPersonFailedLoginAttemptsEntity entity)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();
                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = insertSql;

                command.Parameters.Add(new SqlParameter("PersonId", SqlDbType.Int)).Value = (object)entity.PersonId ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("Count", SqlDbType.Int)).Value = (object)entity.Count ?? DBNull.Value;

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

		public static void Merge([NotNull] BvPersonFailedLoginAttemptsEntity entity)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();
                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = mergeSql;

                command.Parameters.Add(new SqlParameter("PersonId", SqlDbType.Int)).Value = (object)entity.PersonId ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("Count", SqlDbType.Int)).Value = (object)entity.Count ?? DBNull.Value;

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }
		
        [NotNull]
        public static List< BvPersonFailedLoginAttemptsEntity > DeleteByConditionAndOutput(
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;
                
                command.CommandType = CommandType.Text;

                if (condition != null)
                {
                    command.CommandText = deleteAndOutputSql.Replace("%WHERE_CLAUSE%", "\r\nWHERE\r\n" + condition + "\r\n");
                    command.Parameters.AddRange(parameters);
                }
                else
                {
                    command.CommandText = deleteAndOutputSql.Replace("%WHERE_CLAUSE%", "");
                }

                var reader = command.ExecuteReader();
                disposableResources.Push(reader);

                var deletedRows = ReadList(reader);

                return deletedRows;
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }
        
        public static void DeleteByCondition(
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;
                
                command.CommandType = CommandType.Text;
                command.CommandText = deleteSql;

                if (condition != null)
                {
                    command.Parameters.AddRange(parameters);
                    command.CommandText += "\r\nWHERE\r\n" + condition;
                }

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

        public static void Update([NotNull] BvPersonFailedLoginAttemptsEntity entity)
        {
            UpdateByCondition(entity, null);
        }
		
        [NotNull]
        public static List< BvPersonFailedLoginAttemptsEntity > UpdateByConditionAndOutput(
            [NotNull] BvPersonFailedLoginAttemptsEntity entity,
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;

                if (condition != null)
                {
                    command.CommandText = updateAndOutputSql.Replace("%WHERE_CLAUSE%", "\r\nWHERE\r\n" + condition + "\r\n");
                    command.Parameters.AddRange(parameters);
                }
                else
                {
                    command.CommandText = updateAndOutputSql.Replace("%WHERE_CLAUSE%", "");
                }

                command.Parameters.Add(new SqlParameter("PersonId", SqlDbType.Int)).Value = (object)entity.PersonId ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("Count", SqlDbType.Int)).Value = (object)entity.Count ?? DBNull.Value;

                var reader = command.ExecuteReader();
                disposableResources.Push(reader);

                var updatedRows = ReadList(reader);

                return updatedRows;
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }
        
        public static void UpdateByCondition(
            [NotNull] BvPersonFailedLoginAttemptsEntity entity,
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = updateSql;

                if (condition == null)
                {
                    command.CommandText += "\r\nWHERE\r\n" + updateDefaultConditionSql;
                }
                else
                {
                    command.Parameters.AddRange(parameters);
                    command.CommandText += "\r\nWHERE\r\n" + condition;
                }

                command.Parameters.Add(new SqlParameter("PersonId", SqlDbType.Int)).Value = (object)entity.PersonId ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("Count", SqlDbType.Int)).Value = (object)entity.Count ?? DBNull.Value;

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

		[NotNull]
        public static DataTable CreateDataTable()
        {
            var table = new DataTable("BvPersonFailedLoginAttempts");
            var columnPersonId = new DataColumn("PersonId", typeof(int));
            columnPersonId.AllowDBNull = false;
            table.Columns.Add(columnPersonId);

            var columnCount = new DataColumn("Count", typeof(int));
            columnCount.AllowDBNull = false;
            table.Columns.Add(columnCount);

            return table;
        }

        public static void SaveEntity2DataTable(DataTable table, BvPersonFailedLoginAttemptsEntity entity)
        {
            var rowData = new object[]
                {
                    entity.PersonId,
                    entity.Count
                };

            table.Rows.Add(rowData);
        }
    }
}
