//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a EntityAdapterTemplate.tt T4 template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//     
//     DO NOT EDIT THIS FILE MANUALLY
// </auto-generated>
//------------------------------------------------------------------------------

// Disable missing XML comment for publicly visible type or member
#pragma warning disable 1591

using System;
using System.Data;
using System.Data.SqlClient;
using System.Collections.Generic;

using Confirmit.CATI.Common;
using Confirmit.CATI.Core.DAL.Framework;
using Confirmit.CATI.Core.DAL.Generated.Entity.Table;
using Confirmit.CATI.Core.Misc;
using Confirmit.CATI.Common.ServiceLocation;
using Confirmit.CATI.Core.SystemSettings;


namespace Confirmit.CATI.Core.DAL.Generated.Entity.Adapter
{
            
            // TODO: Table BvStartedServices all fields are in the primary key or identity. Generated update statement is not valid.

    public class BvStartedServicesAdapter
    {
        public static readonly string selectSql =
            "SELECT\r\n" +
                "[MachineName],\r\n" +
                "[ServiceName]\r\n" +
            "FROM\r\n" +
                "dbo.[BvStartedServices]";

		public static readonly string mergeSql = 
			"MERGE dbo.[BvStartedServices] as target\r\n" +
			    "USING\r\n" +
			    "(\r\n" + 
			        "VALUES\r\n" +
			        "(\r\n" + 
			            "@MachineName,\r\n" +
                        "@ServiceName" + 
					")" +
				")" +
			    "AS source\r\n" +
				"(\r\n" +
				    "MachineName,\r\n" +
                    "ServiceName" +
				")\r\n" +
			    "ON\r\n" +
				"(\r\n" +
				    "target.[MachineName] = source.[MachineName] AND\r\n" +
                    "target.[ServiceName] = source.[ServiceName]\r\n" +
				")\r\n" +
				"WHEN NOT MATCHED BY TARGET THEN INSERT\r\n" +
				"(\r\n" + 
					"[MachineName],\r\n" +
                    "[ServiceName]\r\n" +
				")\r\n" +
				"VALUES\r\n" +
				"(\r\n" +
					"MachineName,\r\n" +
                    "ServiceName\r\n" +
				");";

        public static readonly string insertSql =
            "INSERT dbo.[BvStartedServices]\r\n"+
            "(\r\n" + 
                "[MachineName],\r\n" +
                "[ServiceName]\r\n" +
            ")\r\n" +
            "VALUES\r\n" +
            "(\r\n" +
                "@MachineName,\r\n" +
                "@ServiceName\r\n" +
            ")";

        public static readonly string deleteSql = 
            "DELETE FROM dbo.[BvStartedServices]\r\n";

        public static readonly string deleteAndOutputSql = 
            "SELECT * INTO #BvStartedServicesOutput FROM dbo.[BvStartedServices] WHERE 1<>1\r\n" +
            "\r\n" +
            "DELETE FROM dbo.[BvStartedServices] OUTPUT DELETED.[MachineName],\r\n" +
                    "DELETED.[ServiceName]\r\n" + 
			"INTO #BvStartedServicesOutput(\r\n" +
                    "[MachineName],\r\n" +
                    "[ServiceName]\r\n" +
			")\r\n" +
			"%WHERE_CLAUSE%" +
            "\r\n" +
            "SELECT\r\n" +
				 "[MachineName],\r\n" +
                "[ServiceName]\r\n" +
            "FROM\r\n" +
             "    #BvStartedServicesOutput\r\n" +
            "\r\n" +
             "DROP TABLE #BvStartedServicesOutput";

        public static readonly string updateSql =
            "UPDATE dbo.[BvStartedServices] SET\r\n" +
                "\r\n";

        public static readonly string updateAndOutputSql =
            "SELECT * INTO #BvStartedServicesOutput FROM dbo.[BvStartedServices] WHERE 1<>1\r\n" +
            "\r\n" +
            "UPDATE dbo.[BvStartedServices] SET\r\n" +
                "\r\n" +
            "OUTPUT INSERTED.* INTO #BvStartedServicesOutput\r\n" +
             "%WHERE_CLAUSE%" +
            "\r\n" +
            "SELECT\r\n" +
                 "[MachineName],\r\n" +
                "[ServiceName]\r\n" +
            "FROM\r\n" +
             "    #BvStartedServicesOutput\r\n" +
            "\r\n" +
             "DROP TABLE #BvStartedServicesOutput";


        public static readonly string updateDefaultConditionSql =
                "[MachineName] = @MachineName AND\r\n" +
                "[ServiceName] = @ServiceName\r\n";
		
		public static readonly bool IsUpdateSupported = false;

        public static BvStartedServicesEntity Read(IDataReader rd)
        {
            bool isRead = rd.Read();

            if (isRead == false)
                return null;

			return ReadEntity(rd);
		}

		public static BvStartedServicesEntity ReadEntity(IDataReader rd)
		{
            var entity = new BvStartedServicesEntity();

            int MachineNameOrdinal = rd.GetOrdinal("MachineName");

            if (!rd.IsDBNull(MachineNameOrdinal))
                entity.MachineName = 
                    rd.GetString(MachineNameOrdinal);

            int ServiceNameOrdinal = rd.GetOrdinal("ServiceName");

            if (!rd.IsDBNull(ServiceNameOrdinal))
                entity.ServiceName = 
                    rd.GetString(ServiceNameOrdinal);

            return entity;
        }

        [NotNull]
        public static List< BvStartedServicesEntity > ReadList(IDataReader rd)
        {
            var BvStartedServicesEntityList = new List< BvStartedServicesEntity >();

            int MachineNameOrdinal = rd.GetOrdinal("MachineName");
            int ServiceNameOrdinal = rd.GetOrdinal("ServiceName");

            while(true)
            {
                bool isRead = rd.Read();

                if (isRead == false)
                    break;

                var entity = new BvStartedServicesEntity();

                if (!rd.IsDBNull(MachineNameOrdinal))
                    entity.MachineName = 
                        rd.GetString(MachineNameOrdinal);

                if (!rd.IsDBNull(ServiceNameOrdinal))
                    entity.ServiceName = 
                        rd.GetString(ServiceNameOrdinal);


                BvStartedServicesEntityList.Add(entity);
            }

            return BvStartedServicesEntityList;
        }
		
        [NotNull]
        public static List< BvStartedServicesEntity > GetAll()
        {
            return GetByCondition(null);
        }
		
        [NotNull]
        public static List< BvStartedServicesEntity > GetAllForCache()
        {
            return GetByCondition(null, true);
        }
		
        [NotNull]
        public static List< BvStartedServicesEntity > GetByCondition(
            string condition,
            params SqlParameter[] parameters)
        {
             return GetByCondition(condition, false, parameters);
        }
		
        [NotNull]
        public static List< BvStartedServicesEntity > GetByCondition(
            string condition,
            bool useNewConnection,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                if(useNewConnection)
                {
                    var connection = new SqlConnection(BackendInstance.Current.ConnectionString);
                    disposableResources.Push(connection);
                    connection.Open();
                    command.Connection = connection;
                }
                else
                {
                    var connectionScope = new ConnectionScope();
                    disposableResources.Push(connectionScope);

                    command.Connection = connectionScope.Connection;

                    if (DatabaseTransactionScope.Current != null)
                    {
                        command.Transaction = DatabaseTransactionScope.Current.Transaction;
                    }
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = selectSql;

                if (condition != null)
                {
                    command.Parameters.AddRange(parameters);
                    command.CommandText += "\r\nWHERE\r\n" + condition;
                }

                var reader = command.ExecuteReader();

                disposableResources.Push(reader);

                return ReadList(reader);
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

        public static void Insert([NotNull] BvStartedServicesEntity entity)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();
                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = insertSql;

                command.Parameters.Add(new SqlParameter("MachineName", SqlDbType.NVarChar)).Value = (object)entity.MachineName ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("ServiceName", SqlDbType.NVarChar)).Value = (object)entity.ServiceName ?? DBNull.Value;

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

		public static void Merge([NotNull] BvStartedServicesEntity entity)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();
                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = mergeSql;

                command.Parameters.Add(new SqlParameter("MachineName", SqlDbType.NVarChar)).Value = (object)entity.MachineName ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("ServiceName", SqlDbType.NVarChar)).Value = (object)entity.ServiceName ?? DBNull.Value;

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }
		
        [NotNull]
        public static List< BvStartedServicesEntity > DeleteByConditionAndOutput(
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;
                
                command.CommandType = CommandType.Text;

                if (condition != null)
                {
                    command.CommandText = deleteAndOutputSql.Replace("%WHERE_CLAUSE%", "\r\nWHERE\r\n" + condition + "\r\n");
                    command.Parameters.AddRange(parameters);
                }
                else
                {
                    command.CommandText = deleteAndOutputSql.Replace("%WHERE_CLAUSE%", "");
                }

                var reader = command.ExecuteReader();
                disposableResources.Push(reader);

                var deletedRows = ReadList(reader);

                return deletedRows;
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }
        
        public static void DeleteByCondition(
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;
                
                command.CommandType = CommandType.Text;
                command.CommandText = deleteSql;

                if (condition != null)
                {
                    command.Parameters.AddRange(parameters);
                    command.CommandText += "\r\nWHERE\r\n" + condition;
                }

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

        public static void Update([NotNull] BvStartedServicesEntity entity)
        {
            UpdateByCondition(entity, null);
        }
		
        [NotNull]
        public static List< BvStartedServicesEntity > UpdateByConditionAndOutput(
            [NotNull] BvStartedServicesEntity entity,
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;

                if (condition != null)
                {
                    command.CommandText = updateAndOutputSql.Replace("%WHERE_CLAUSE%", "\r\nWHERE\r\n" + condition + "\r\n");
                    command.Parameters.AddRange(parameters);
                }
                else
                {
                    command.CommandText = updateAndOutputSql.Replace("%WHERE_CLAUSE%", "");
                }

                command.Parameters.Add(new SqlParameter("MachineName", SqlDbType.NVarChar)).Value = (object)entity.MachineName ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("ServiceName", SqlDbType.NVarChar)).Value = (object)entity.ServiceName ?? DBNull.Value;

                var reader = command.ExecuteReader();
                disposableResources.Push(reader);

                var updatedRows = ReadList(reader);

                return updatedRows;
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }
        
        public static void UpdateByCondition(
            [NotNull] BvStartedServicesEntity entity,
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = updateSql;

                if (condition == null)
                {
                    command.CommandText += "\r\nWHERE\r\n" + updateDefaultConditionSql;
                }
                else
                {
                    command.Parameters.AddRange(parameters);
                    command.CommandText += "\r\nWHERE\r\n" + condition;
                }

                command.Parameters.Add(new SqlParameter("MachineName", SqlDbType.NVarChar)).Value = (object)entity.MachineName ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("ServiceName", SqlDbType.NVarChar)).Value = (object)entity.ServiceName ?? DBNull.Value;

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

		[NotNull]
        public static DataTable CreateDataTable()
        {
            var table = new DataTable("BvStartedServices");
            var columnMachineName = new DataColumn("MachineName", typeof(string));
            columnMachineName.AllowDBNull = false;
            table.Columns.Add(columnMachineName);

            var columnServiceName = new DataColumn("ServiceName", typeof(string));
            columnServiceName.AllowDBNull = false;
            table.Columns.Add(columnServiceName);

            return table;
        }

        public static void SaveEntity2DataTable(DataTable table, BvStartedServicesEntity entity)
        {
            var rowData = new object[]
                {
                    entity.MachineName,
                    entity.ServiceName
                };

            table.Rows.Add(rowData);
        }
    }
}
