//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a EntityAdapterTemplate.tt T4 template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//     
//     DO NOT EDIT THIS FILE MANUALLY
// </auto-generated>
//------------------------------------------------------------------------------

// Disable missing XML comment for publicly visible type or member
#pragma warning disable 1591

using System;
using System.Data;
using System.Data.SqlClient;
using System.Collections.Generic;

using Confirmit.CATI.Common;
using Confirmit.CATI.Core.DAL.Framework;
using Confirmit.CATI.Core.DAL.Generated.Entity.Table;
using Confirmit.CATI.Core.Misc;
using Confirmit.CATI.Common.ServiceLocation;
using Confirmit.CATI.Core.SystemSettings;


namespace Confirmit.CATI.Core.DAL.Generated.Entity.Adapter
{
            
            

    public class BvScheduledEmailReportsAdapter
    {
        public static readonly string selectSql =
            "SELECT\r\n" +
                "[ReportType],\r\n" +
                "[LastSent]\r\n" +
            "FROM\r\n" +
                "dbo.[BvScheduledEmailReports]";

		public static readonly string mergeSql = 
			"MERGE dbo.[BvScheduledEmailReports] as target\r\n" +
			    "USING\r\n" +
			    "(\r\n" + 
			        "VALUES\r\n" +
			        "(\r\n" + 
			            "@ReportType,\r\n" +
                        "@LastSent" + 
					")" +
				")" +
			    "AS source\r\n" +
				"(\r\n" +
				    "ReportType,\r\n" +
                    "LastSent" +
				")\r\n" +
			    "ON\r\n" +
				"(\r\n" +
				    "target.[ReportType] = source.[ReportType]\r\n" +
				")\r\n" +
                "WHEN MATCHED THEN UPDATE SET\r\n" +
					"target.[LastSent] = source.[LastSent]\r\n" +
				"WHEN NOT MATCHED BY TARGET THEN INSERT\r\n" +
				"(\r\n" + 
					"[ReportType],\r\n" +
                    "[LastSent]\r\n" +
				")\r\n" +
				"VALUES\r\n" +
				"(\r\n" +
					"ReportType,\r\n" +
                    "LastSent\r\n" +
				");";

        public static readonly string insertSql =
            "INSERT dbo.[BvScheduledEmailReports]\r\n"+
            "(\r\n" + 
                "[ReportType],\r\n" +
                "[LastSent]\r\n" +
            ")\r\n" +
            "VALUES\r\n" +
            "(\r\n" +
                "@ReportType,\r\n" +
                "@LastSent\r\n" +
            ")";

        public static readonly string deleteSql = 
            "DELETE FROM dbo.[BvScheduledEmailReports]\r\n";

        public static readonly string deleteAndOutputSql = 
            "SELECT * INTO #BvScheduledEmailReportsOutput FROM dbo.[BvScheduledEmailReports] WHERE 1<>1\r\n" +
            "\r\n" +
            "DELETE FROM dbo.[BvScheduledEmailReports] OUTPUT DELETED.[ReportType],\r\n" +
                    "DELETED.[LastSent]\r\n" + 
			"INTO #BvScheduledEmailReportsOutput(\r\n" +
                    "[ReportType],\r\n" +
                    "[LastSent]\r\n" +
			")\r\n" +
			"%WHERE_CLAUSE%" +
            "\r\n" +
            "SELECT\r\n" +
				 "[ReportType],\r\n" +
                "[LastSent]\r\n" +
            "FROM\r\n" +
             "    #BvScheduledEmailReportsOutput\r\n" +
            "\r\n" +
             "DROP TABLE #BvScheduledEmailReportsOutput";

        public static readonly string updateSql =
            "UPDATE dbo.[BvScheduledEmailReports] SET\r\n" +
                "[LastSent] = @LastSent\r\n";

        public static readonly string updateAndOutputSql =
            "SELECT * INTO #BvScheduledEmailReportsOutput FROM dbo.[BvScheduledEmailReports] WHERE 1<>1\r\n" +
            "\r\n" +
            "UPDATE dbo.[BvScheduledEmailReports] SET\r\n" +
                "[LastSent] = @LastSent\r\n" +
            "OUTPUT INSERTED.* INTO #BvScheduledEmailReportsOutput\r\n" +
             "%WHERE_CLAUSE%" +
            "\r\n" +
            "SELECT\r\n" +
                 "[ReportType],\r\n" +
                "[LastSent]\r\n" +
            "FROM\r\n" +
             "    #BvScheduledEmailReportsOutput\r\n" +
            "\r\n" +
             "DROP TABLE #BvScheduledEmailReportsOutput";


        public static readonly string updateDefaultConditionSql =
                "[ReportType] = @ReportType\r\n";
		
		public static readonly bool IsUpdateSupported = true;

        public static BvScheduledEmailReportsEntity Read(IDataReader rd)
        {
            bool isRead = rd.Read();

            if (isRead == false)
                return null;

			return ReadEntity(rd);
		}

		public static BvScheduledEmailReportsEntity ReadEntity(IDataReader rd)
		{
            var entity = new BvScheduledEmailReportsEntity();

            int ReportTypeOrdinal = rd.GetOrdinal("ReportType");

            if (!rd.IsDBNull(ReportTypeOrdinal))
                entity.ReportType = 
                    rd.GetInt32(ReportTypeOrdinal);

            int LastSentOrdinal = rd.GetOrdinal("LastSent");

            if (!rd.IsDBNull(LastSentOrdinal))
                entity.LastSent = 
                    rd.GetDateTime(LastSentOrdinal);

            return entity;
        }

        [NotNull]
        public static List< BvScheduledEmailReportsEntity > ReadList(IDataReader rd)
        {
            var BvScheduledEmailReportsEntityList = new List< BvScheduledEmailReportsEntity >();

            int ReportTypeOrdinal = rd.GetOrdinal("ReportType");
            int LastSentOrdinal = rd.GetOrdinal("LastSent");

            while(true)
            {
                bool isRead = rd.Read();

                if (isRead == false)
                    break;

                var entity = new BvScheduledEmailReportsEntity();

                if (!rd.IsDBNull(ReportTypeOrdinal))
                    entity.ReportType = 
                        rd.GetInt32(ReportTypeOrdinal);

                if (!rd.IsDBNull(LastSentOrdinal))
                    entity.LastSent = 
                        rd.GetDateTime(LastSentOrdinal);


                BvScheduledEmailReportsEntityList.Add(entity);
            }

            return BvScheduledEmailReportsEntityList;
        }
		
        [NotNull]
        public static List< BvScheduledEmailReportsEntity > GetAll()
        {
            return GetByCondition(null);
        }
		
        [NotNull]
        public static List< BvScheduledEmailReportsEntity > GetAllForCache()
        {
            return GetByCondition(null, true);
        }
		
        [NotNull]
        public static List< BvScheduledEmailReportsEntity > GetByCondition(
            string condition,
            params SqlParameter[] parameters)
        {
             return GetByCondition(condition, false, parameters);
        }
		
        [NotNull]
        public static List< BvScheduledEmailReportsEntity > GetByCondition(
            string condition,
            bool useNewConnection,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                if(useNewConnection)
                {
                    var connection = new SqlConnection(BackendInstance.Current.ConnectionString);
                    disposableResources.Push(connection);
                    connection.Open();
                    command.Connection = connection;
                }
                else
                {
                    var connectionScope = new ConnectionScope();
                    disposableResources.Push(connectionScope);

                    command.Connection = connectionScope.Connection;

                    if (DatabaseTransactionScope.Current != null)
                    {
                        command.Transaction = DatabaseTransactionScope.Current.Transaction;
                    }
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = selectSql;

                if (condition != null)
                {
                    command.Parameters.AddRange(parameters);
                    command.CommandText += "\r\nWHERE\r\n" + condition;
                }

                var reader = command.ExecuteReader();

                disposableResources.Push(reader);

                return ReadList(reader);
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

        public static void Insert([NotNull] BvScheduledEmailReportsEntity entity)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();
                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = insertSql;

                command.Parameters.Add(new SqlParameter("ReportType", SqlDbType.Int)).Value = (object)entity.ReportType ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("LastSent", SqlDbType.DateTime)).Value = (object)entity.LastSent ?? DBNull.Value;

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

		public static void Merge([NotNull] BvScheduledEmailReportsEntity entity)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();
                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = mergeSql;

                command.Parameters.Add(new SqlParameter("ReportType", SqlDbType.Int)).Value = (object)entity.ReportType ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("LastSent", SqlDbType.DateTime)).Value = (object)entity.LastSent ?? DBNull.Value;

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }
		
        [NotNull]
        public static List< BvScheduledEmailReportsEntity > DeleteByConditionAndOutput(
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;
                
                command.CommandType = CommandType.Text;

                if (condition != null)
                {
                    command.CommandText = deleteAndOutputSql.Replace("%WHERE_CLAUSE%", "\r\nWHERE\r\n" + condition + "\r\n");
                    command.Parameters.AddRange(parameters);
                }
                else
                {
                    command.CommandText = deleteAndOutputSql.Replace("%WHERE_CLAUSE%", "");
                }

                var reader = command.ExecuteReader();
                disposableResources.Push(reader);

                var deletedRows = ReadList(reader);

                return deletedRows;
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }
        
        public static void DeleteByCondition(
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;
                
                command.CommandType = CommandType.Text;
                command.CommandText = deleteSql;

                if (condition != null)
                {
                    command.Parameters.AddRange(parameters);
                    command.CommandText += "\r\nWHERE\r\n" + condition;
                }

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

        public static void Update([NotNull] BvScheduledEmailReportsEntity entity)
        {
            UpdateByCondition(entity, null);
        }
		
        [NotNull]
        public static List< BvScheduledEmailReportsEntity > UpdateByConditionAndOutput(
            [NotNull] BvScheduledEmailReportsEntity entity,
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;

                if (condition != null)
                {
                    command.CommandText = updateAndOutputSql.Replace("%WHERE_CLAUSE%", "\r\nWHERE\r\n" + condition + "\r\n");
                    command.Parameters.AddRange(parameters);
                }
                else
                {
                    command.CommandText = updateAndOutputSql.Replace("%WHERE_CLAUSE%", "");
                }

                command.Parameters.Add(new SqlParameter("ReportType", SqlDbType.Int)).Value = (object)entity.ReportType ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("LastSent", SqlDbType.DateTime)).Value = (object)entity.LastSent ?? DBNull.Value;

                var reader = command.ExecuteReader();
                disposableResources.Push(reader);

                var updatedRows = ReadList(reader);

                return updatedRows;
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }
        
        public static void UpdateByCondition(
            [NotNull] BvScheduledEmailReportsEntity entity,
            string condition,
            params SqlParameter[] parameters)
        {
            var disposableResources = new Stack<IDisposable>();

            try
            {
                var command = new SqlCommand();

                var connectionScope = new ConnectionScope();
                disposableResources.Push(connectionScope);
                command.Connection = connectionScope.Connection;

                if (DatabaseTransactionScope.Current != null)
                {
                    command.Transaction = DatabaseTransactionScope.Current.Transaction;
                }

                disposableResources.Push(command);

                command.CommandTimeout = Framework.Constants.DefaultDatabaseCommandTimeout;

                command.CommandType = CommandType.Text;
                command.CommandText = updateSql;

                if (condition == null)
                {
                    command.CommandText += "\r\nWHERE\r\n" + updateDefaultConditionSql;
                }
                else
                {
                    command.Parameters.AddRange(parameters);
                    command.CommandText += "\r\nWHERE\r\n" + condition;
                }

                command.Parameters.Add(new SqlParameter("ReportType", SqlDbType.Int)).Value = (object)entity.ReportType ?? DBNull.Value;
                command.Parameters.Add(new SqlParameter("LastSent", SqlDbType.DateTime)).Value = (object)entity.LastSent ?? DBNull.Value;

                command.ExecuteNonQuery();
            }
            finally
            {
                while(disposableResources.Count != 0)
                {
                    var resource2Dispose = disposableResources.Pop();

                    resource2Dispose.Dispose();
                }
            }
        }

		[NotNull]
        public static DataTable CreateDataTable()
        {
            var table = new DataTable("BvScheduledEmailReports");
            var columnReportType = new DataColumn("ReportType", typeof(int));
            columnReportType.AllowDBNull = false;
            table.Columns.Add(columnReportType);

            var columnLastSent = new DataColumn("LastSent", typeof(DateTime));
            columnLastSent.AllowDBNull = true;
            table.Columns.Add(columnLastSent);

            return table;
        }

        public static void SaveEntity2DataTable(DataTable table, BvScheduledEmailReportsEntity entity)
        {
            var rowData = new object[]
                {
                    entity.ReportType,
                    entity.LastSent
                };

            table.Rows.Add(rowData);
        }
    }
}
